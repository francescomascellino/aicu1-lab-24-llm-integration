# L24 multi-ticket summary student lab instructions

Complete only the three ordered TODOs in `src/incident.js`.

- Use pnpm only; keep the project runnable on macOS, Windows, and Linux.
- Do not change backend routes, validators, tests, or the SQLite schema.
- Use the existing API and view helpers instead of duplicating them.
- Treat generated content as a preview until the user explicitly saves it.
- Never send database access, invented ticket IDs, or provider secrets.
- Do not add silent fallbacks, new dependencies, or unrelated UI features.
- Replay is the required path; OpenCode and Mistral are optional.
- Run `pnpm exercise:l24` while working and `pnpm verify:l24` when complete.
